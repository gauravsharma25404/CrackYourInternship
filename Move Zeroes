void moveZeroes(vector<int>& nums) {
        int n= nums.size();
             vector<int> temp;
             int k=0;
             for(int i=0;i<n;i++){
                 if(nums[i]!=0){
                     temp.push_back(nums[i]);
                     k++;
                 }
             }
             for(int i=k;i<n;i++){
                 temp.push_back(0);
             }
             nums=temp;
    }
